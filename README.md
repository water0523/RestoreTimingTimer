# timing 计时器（RestoreTimingTimer）

**v1.4.2**（本地版本；GitHub Pages 上目前跑的是 v1.4.1）

单文件（`index.html`）的沉浸式学习计时器：设定时长 → 全屏背景 + 进度环 → 到点提醒 → 记录 / 导出。
所有可选项都在设置弹窗里（默认全开），云同步已在 v1.3.0 移除。
版本号也显示在设置弹窗底部（齿轮图标），单一来源是 `index.html` 里的 `APP_VERSION`。

在线地址：<https://water0523.github.io/RestoreTimingTimer/>

> 仓库设置里的主页字段目前还是改名前的 `https://water0523.github.io/timing-/`（已 404），
> 建议在 GitHub 仓库设置里改成上面这个地址。

## 本地使用（桌面浏览器）

直接双击 `index.html` 就能用，所有数据保存在浏览器 localStorage 里，不会自动上传。

也可以起个本地静态服务（更接近线上环境）：

```bash
python -m http.server 8000     # 然后打开 http://localhost:8000/
```

## 功能要点

- **保持屏幕常亮**（v1.2.9 起，默认开启）：只要人还停在计时器这个页面上，就不让屏幕休眠
  （用的是浏览器的 Screen Wake Lock 接口，计时中、暂停中都算）。切到别的标签页、最小化或锁屏时自动放开，
  回到本页面再自动接管。设置弹窗（⚙）里可以关掉这个设定；浏览器不支持时开关会置灰，不会假装生效。
- **标签页标题实时显示剩余时间**：`25:00 · 背单词`，暂停时变成 `已暂停 · 背单词`，切到别的标签页也能看到进度。
- **顶部毛玻璃当前时间**（v1.2.8 起）：开始计时后，鼠标一动就在屏幕正上方浮出一枚苹果风毛玻璃圆角药丸，
  显示当前时间（`HH:MM:SS`，精确到秒）；鼠标停下 3 秒后自动渐隐（0.4s 过渡），期间继续移动会重新计时。
  浮层是 `pointer-events: none`，永远不会挡住"结束 / 暂停"按钮；弹窗打开时一律不显示。
  秒级定时器只在浮层可见时挂，并且对齐真实整秒（不会越走越慢）。
- **记住上次设置**：任务名和学习时长会记下来，下次打开自动回填（时长只提示"已记住上次设置"，不写死结束时间）。
- **到点提醒**：总结弹窗 + 标签页标题闪烁 `⏰ 时间到！`。**本应用不播放任何声音**，没有音效代码。
- **历史记录合计**：列表顶部吸顶显示"共 N 条 · 今日 X · 本周 X"与"累计 X"。
- **历史记录可改名**：在历史里点一下任务名就能改（`Enter` 保存 / `Esc` 取消 / 点别处保存），
  只改标题，时长、开始时间和备注都不动。
- **专注热力图**：历史记录里有一张类似 GitHub 贡献图的热力图（最近 18 周，列 = 周、行 = 星期）：
  灰 = 当天没用过，蓝 = 用过，紫 = 当天超过 4 小时，红 = **当天达到 8 小时**（单次时长上限就是 8 小时，
  所以红是"满 8 小时"而不是"超 8 小时"；一天里多段累加也算）；
  点任意圆点会在下方显示那天的日期、记录条数和当天总时长；顶部有颜色图例和 `?` 说明。
- **连续使用天数**：热力图右上角显示连续使用的倍数徽章 `×N`（第 1 天用浅色"连续 1 天"提示）。
  今天还没开始不算断，会从昨天继续往前数；断掉后徽章消失。
- **任务建议**：用过的任务都会进建议（按使用次数排序，最多 8 条），输入时按内容模糊筛选（输入"背词"也能命中"背单词"）。
- **暂存 / 续上一次**：一场专注结束时，弹窗里除了"保存记录 / 不保存"还有"暂存"——
  把已用时长和任务名存下来（**不写记录**），回到主页会出现提示条「上次的『背单词』还剩 18 分 30 秒」，
  点"继续"接着跑，点"丢弃"清掉。暂存期间的时间不算进专注时长；续上之后再结束，
  记录里的开始时间仍然是原来那次的时间。
  （同一时间只保留一份暂存，再次暂存会覆盖；暂存内容存在浏览器本地。）
- **自动恢复（防刷新 / 崩溃）**：计时进行中每 5 秒把进度写进浏览器本地，切到后台、关闭页面前也会各补一次；
  刷新页面、误关标签页、浏览器崩溃后回到主页，提示条会写「上次没结束的『背单词』还剩 12 分 10 秒」，
  点"继续"接着跑，最多只丢几秒。手动"暂存"和自动快照可以同时存在，提示条显示较新的那一份。
- **进度环按 20 分钟一圈**，每圈自动切换一张背景图；切到后台再回来会按真实进度对齐图片。
  切图节点除了帧循环还有 `setTimeout` 兜底，窗口被遮挡/切后台导致 rAF 被节流时也不会漏；
  需要的话按 `右 Ctrl + →` 可以手动强制切下一张。
- **背景图加载策略**（v1.2.6 起）：
  - 计时中在**下一个 20 分钟节点到来前 90 秒预取"下一张"**——到点切图零等待，开场也不会一下吃 7.4MB；
  - 回到主页空闲时再把 24 张慢慢预热；一进计时或开发者模式就让路，不抢带宽；
  - 线上用 **Service Worker** 把背景图永久缓存在本地（GitHub Pages 只给 `max-age=600`，10 分钟就过期，
    而计时器 20 分钟才切一次图）——首次访问之后**断网也能正常切图**，开发者模式来回跳也是秒切。
- **暂停是真的暂停**：计时、进度环、文字呼吸、背景平移与三圈波纹动画全部冻结。
  文字呼吸的相位由"已用时长"推导（`(已用时长 - 10s) % 20s`），所以暂停再恢复不会和 20 秒节奏错位，
  恢复时也不会重播开场翻转动画；暂停期间不会再去加载新的背景图。
- **背景平移与文字呼吸严格对齐**（v1.4.2 起）：背景的左右平移不再是 CSS 关键帧动画
  （那种动画从"元素渲染那一刻"按墙钟跑，刷新页面 / 切后台再回来 / 暂停恢复之后就和时间轴对不上），
  而是每帧和文字呼吸一样按"已用时长"算出来。节奏：单程 20 秒、一去一回 40 秒，
  **起点（已用 0 秒）在最右边**，折返只发生在 20 秒的整数倍上：
  0s = 最右（开始向左）→ 20s = 最左（折返）→ 40s = 最右（一个完整循环，再次折返）。
  文字每 10 秒切换一次，所以每次折返、每次走到一半，都恰好压在文字切换的那一刻；
  20 分钟换图时"已用时长"必然是 40 秒的整数倍，每张新图都从"最右 + 整循环起点"开始。

## 快捷键

| 按键 | 作用 |
| --- | --- |
| `空格` | 暂停 / 继续（计时页） |
| `右 Ctrl + →` | 强制切到下一张背景图（不影响 20 分钟基准，下一个节点照常再切一张） |
| `Esc` | 关闭最上层弹窗（总结弹窗按 Esc 视为"不保存"） |
| `Shift + Enter` | 进入 / 退出全屏 |
| 页面空白处三连击 | 进入全屏 |
| 任务名输入框里输入 `water` 再回车 | 开发者模式（8 小时时间轴，调试用；计时页显示当前时期名） |

计时进行中刷新或关闭页面会弹确认框，避免误操作丢掉整段专注时间。

## 目录说明

- `index.html`：整个应用（HTML + CSS + JS 单文件）
- `images/*.webp`：24 张背景图，每 20 分钟自动切换一张
- `images/*.png` / `images/12.jpg`：压缩前的原图（约 141MB），确认画质满意后可以删掉
- `sw.js`：Service Worker（背景图永久缓存 + 页面网络优先；只在 https 生效，`file://` 自动跳过）

> ⚠️ **换了 `images/` 下的图片后，记得把 `sw.js` 里的 `CACHE_VERSION` 加一**，
> 否则已经装过 Service Worker 的访客会继续用缓存里的旧图。

## 设置（⚙ 齿轮图标）

所有"可以自己选"的功能都放在设置弹窗里，分四组，**默认全部打开**，改动立刻生效并存在本设备的
localStorage（键名 `timing_prefs`，一条 JSON）。

| 分组 | 开关 | 关掉之后 |
| --- | --- | --- |
| 通用 | 保持屏幕常亮 | 页面开着也不阻止屏幕休眠（浏览器不支持时该项置灰） |
| 通用 | 顶部毛玻璃当前时间 | 计时中移动鼠标不再浮出当前时间 |
| 计时与提醒 | 标签页标题显示剩余时间 | 标题保持「timing计时器」，不再显示倒计时 |
| 计时与提醒 | 到点时闪烁标签页标题 | 时间到只弹总结弹窗，标题不闪 |
| 计时与提醒 | 记住上次的任务名与时长 | 下次打开不回填，并清掉已记住的内容 |
| 计时与提醒 | 自动恢复未结束的计时 | 不再写 5 秒一次的快照（手动「暂存」仍然可用） |
| 历史与统计 | 历史记录顶部显示合计 | 隐藏「共 N 条 · 今日 / 本周 / 累计」 |
| 历史与统计 | 专注热力图与连续天数 | 隐藏热力图与 `×N` 徽章（记录列表本身照常） |
| 历史与统计 | 任务名建议 | 输入任务名时不弹下拉建议 |
| 性能与网络 | 提前预取下一张背景图 | 不提前下载，20 分钟节点到点才取图 |
| 性能与网络 | 空闲时预加载全部背景图 | 停在主页时不再缓存那 7.4MB |
| 数据备份 | 每次保存记录时写入备份文件 | 保存后不自动写备份文件（「立即写入一次」仍可用） |

### 固定位置的备份文件（数据备份）

在设置里点「选择 / 更改备份文件」选一个 `.json`（建议放到网盘的同步文件夹里），浏览器会记住这个文件；
之后**每次保存记录都会把新数据合并写进这同一个文件**，不会再生成一堆带时间戳的副本：

- **覆盖合并**：同一个 `id` 只保留一条；本机改过任务名以本机为准；文件里本机没有的记录会被保留下来
  （所以两台设备先后各写一次，文件里两份记录都在）。
- **删除同步**：在历史里删掉一条，备份文件里对应那条也会一并移除，免得下次合并又把它带回来。
- 想换位置点「更改文件」；「断开」只是不再写这个文件，本机记录不会被动。
- 前提：网页没有权限直接往任意磁盘路径写文件，所以必须由你**选一次**文件（Chrome / Edge 支持，
  本地双击 `index.html` 也能用）。浏览器不支持时会提示改用历史记录里的「导出备份」（下载一份
  `timing-backup.json`）。选过之后浏览器可能偶尔要求重新授权，点一下「立即写入一次」即可。

> **云同步已在 v1.3.0 整体移除**（GitHub Token、仓库配置、`data/records.json` 读写全部删掉）。
> 老版本存在浏览器里的 Token 会在升级后第一次打开页面时被自动清除。
> 跨设备搬记录请用历史记录弹窗里的「📤 导出备份 / 📥 导入备份」，或者上面这个固定备份文件。

## 已知限制

- 计时过程中刷新页面的情况已被"自动恢复"覆盖：最多丢 5 秒，回主页点"继续"即可接着跑。
- 只针对桌面浏览器做了适配与快捷键，手机/平板未做验证。
- 电脑休眠/睡眠期间 `performance.now()` 是否继续走，取决于系统与浏览器实现，未做唤醒校准。

## 更新日志

### v1.4.2（本地版本，尚未推送）

- 修复：**背景平移和文字呼吸对不齐**（刷新页面 / 切到后台再回来 / 暂停恢复之后特别明显）。
  原因：文字呼吸的相位是**按"已用时长"算**的（所以它总能对上），
  而背景平移是一句 CSS 关键帧动画（`animation: moveBg 20s linear infinite alternate`）——
  它从"元素第一次渲染"开始按墙钟自己跑，和"已用时长"没有任何关系，两者各走各的。
- 改法：去掉那句 CSS 动画，改成每帧用 `syncBackgroundPan()` 按"已用时长"算位置，
  和文字呼吸共用同一条时间轴。节奏不变（单程 20 秒、一去一回 40 秒），
  **起点（已用 0 秒）在最右边**，折返只发生在 20 秒的整数倍上：
  | 已用时长 | 文字 | 图片位置 |
  | --- | --- | --- |
  | 0s | 显示（开场） | **100% 最右**，从这里开始向左走 |
  | 10s | 隐藏 | 50%（走到一半） |
  | 20s | 显示 | **0% 最左，方向在这里折返** |
  | 30s | 隐藏 | 50% |
  | 40s | 显示 | **100% 最右，一个完整循环，再次折返** |
  于是每 40 秒正好一个完整循环，折返只出现在 20 秒的整数倍（0/20/40/60…），
  不会出现"十位是奇数时折返"这种怪现象；而文字每 10 秒切换一次，
  所以每次折返、每次走到一半，都恰好压在文字切换的那一刻。
  另外每 20 分钟换图时"已用时长"是 20 分钟的整数倍，必然也是 40 秒的整数倍，
  所以每张新图都从"最右 + 一个完整循环的起点"开始，永远整齐。
  实测（真实 DOM 采样）：已用 0/20/40/60 秒时图片在 99.7% / 0.25% / 99.7% / 0.25%，
  10/30/50 秒时在 50% 附近，与理论值一致。
- 顺带：暂停时背景平移依然会停（它现在由每帧驱动，暂停时 rAF 不跑，位置自然冻住）。
- 开发者模式复查：拖动时间轴（含跨节点、往回拖）时图片、时期名、平移位置三者一致；
  拖动期间文字保持常亮、松手 1 秒后交回呼吸相位；`exitDevMode` 后不残留 dev-mode 类、
  也不会留下幽灵会话。这套逻辑在时间轴下没有发现额外问题（新增 12 条测试覆盖）。
- 版本号 1.4.2。

### v1.4.1（已发布到 GitHub Pages）

- 修复：**每 20 分钟自动切图的那一瞬间，背景会"闪一下"**。
  原因：原来只有一层背景，直接把 `background-image` 换掉；这些图都是 2560px 的大图，
  浏览器要现场解码，解码的那一帧背景是空的（黑一下），看起来就是闪。
  现在改成**两层背景轮换**：新图先铺在下面那一层，**等 `img.decode()` 解码完成**再淡入，
  旧图同时淡出（0.8s 交叉淡化）。实测切换过程中两层不透明度之和恒为 0.80，
  也就是全程屏幕上都有完整背景，不再有空帧。
  （顺带发现：原来那句 `transition: background-image 1s` 是死代码——Chrome 并不对
   `background-image` 做补间，实测直接就是硬切，所以以前也没有真正的淡入淡出。）
- 修复：**切图那一瞬间会先冒出"上一个阶段"的文字，再跳到新阶段**。
  原因有两个，叠在一起：
  1. 隐藏旧文字用的是 CSS 里 1.5s 的透明度过渡，而 1 秒后就把内容换成新的了 ——
     新旧文字会有一段时间重叠/先后出现。现在换字时临时关掉过渡，让旧文字**瞬间消失**。
  2. 更要命的是：rAF 每帧都会调 `syncBreathing()`，相位一算到"该显示"，
     就把刚藏起来的**旧时期名**重新放了出来。现在换字期间会短暂屏蔽相位干预（`STAGE_SWITCH_MS = 400ms`），
     等新内容写进去之后再交回给相位决定显示还是隐藏（同一次 tick 内完成，不会闪一帧）。
  最终效果：切换瞬间旧文字立刻消失 → 400ms 后新阶段文字淡入。
- 版本号 1.4.1。

### v1.4.0（已发布到 GitHub Pages）

- 新增：**固定位置的备份文件**。设置里新增「数据备份」分组，选一次 `.json` 文件（浏览器记住它），
  之后每次保存记录都会**覆盖合并**写进这同一个文件：同一个 `id` 只留一条、本机改动优先、
  文件里独有的记录保留；删除记录时也会从备份文件里同步移除。
  用的是 File System Access API（`showSaveFilePicker` + IndexedDB 存句柄），
  Chrome / Edge 支持，本地 `file://` 同样可用；不支持时提示改用「导出备份」。
  手动「暂存 / 不保存」不会写备份文件，只有真正保存成记录才会。
- 调整：「导出备份」的文件名从带日期的 `Timing_备份_YYYYMMDD.json` 改成固定的 `timing-backup.json`。
- 版本号 1.4.0。

### v1.3.0（已发布到 GitHub Pages）

- **移除：云同步功能**。GitHub Token / 仓库 / 分支配置、`data/records.json` 的读（合并）写（上传）
  全部删除，"查看历史记录"现在只读本地。升级后第一次打开页面会自动清掉老版本留在 localStorage 里的
  Token（`github_token` / `github_repo` / `github_branch`），不再有明文 Token 躺在浏览器里。
  跨设备搬记录仍然可以用「导出备份 / 导入备份」。
- 新增：**设置弹窗改成四个分组的开关面板**，所有可选项都在里面，**默认全部打开**：
  通用（保持屏幕常亮、顶部毛玻璃当前时间）、计时与提醒（标签页剩余时间、到点闪烁标题、
  记住上次设置、自动恢复）、历史与统计（顶部合计、热力图与连续天数、任务名建议）、
  性能与网络（提前预取下一张、空闲时预加载全部）。
  偏好存成一条 JSON（`timing_prefs`）；关掉某个开关会立刻收起已经生效的效果（比如关掉标题倒计时，
  标题立刻恢复原样；关掉自动恢复会清掉快照），重新打开会立刻补回来。
  兼容 v1.2.9 单独存的 `timing_keepScreenAwake`。

### v1.2.9（本地版本，尚未推送）

- 新增：**保持屏幕常亮**。人停在计时器页面上时不让屏幕休眠（Screen Wake Lock 接口），
  切走/最小化/锁屏自动放开，回来自动接管；设置弹窗里新增「通用」分组和这个开关（默认开启，
  存 `timing_keepScreenAwake`），浏览器不支持时开关置灰。设置弹窗标题相应从"云同步设置"改成"⚙️ 设置"。
- 调整：顶部毛玻璃当前时间下移到 `top: 132px`、字号 22px → 25px、玻璃底色减淡（更通透）；
  开发者模式下自动再下移到 156px，避开顶部时间轴面板。

### v1.2.8（本地版本，尚未推送）

- 新增：**顶部毛玻璃当前时间浮层**。开始计时后，鼠标一移动就在屏幕正上方浮出一枚苹果风毛玻璃圆角药丸，
  显示当前时间（`HH:MM:SS`，精确到秒）；鼠标停下 3 秒后自动渐隐。
  细节：秒级定时器只在可见时挂、并对齐真实整秒；浮层 `pointer-events: none` 不挡按钮；
  弹窗打开 / 鼠标移出窗口 / 标签页失焦时立刻收起；不支持 `backdrop-filter` 的浏览器退化成深色半透明底。
  字体沿用主计时数字（细字重 + 字间距 + 等宽数字），秒跳动时不会左右抖。

### v1.2.7（已发布到 GitHub Pages）

- 调整：**热力图红色改为"当天达到 8 小时"**。原来写的是"超过 8 小时"，但单次时长上限就是 8 小时，
  这个条件实际上永远达不到。紫色仍是"超过 4 小时"；图例、`?` 提示同步改成"满 8 小时"。
- 调整：**时长选择器上限收到 8 小时**（原来最多可选 12 小时 59 分），与"上限 8 小时"保持一致；
  旧的本地设置/暂存里如果存着更大的值，会自动收口到 8 小时。

### v1.2.6（已发布到 GitHub Pages）

- 新增（A）**提前量预取**：不再开场就下 7.4MB，而是在下一个节点到来前 90 秒预取"下一张"。
  到点切图零等待、开场没有带宽风暴；暂停中与开发者模式不预取，拖动跳转后会重新锚定。
- 新增（B）**Service Worker 永久缓存背景图**：线上静态资源只有 `max-age=600`（10 分钟），
  而计时器 20 分钟才切一次图，所以每次切图时那张图都已过期、必须重新走一次网络。
  现在图片走 cache-first 存进 Cache Storage（不受 max-age 限制），页面走 network-first 保证拿到新版本；
  首次访问之后线上体验≈本地，开发者模式随便跳，**断网也能切图**。
- 调整：全量预热改为"只在回到主页空闲时进行"，一进计时/开发者模式就让路。
- 修复：进入开发者模式时，已经排好的预取定时器没有清掉（已加回归测试）。

### v1.2.5（已发布到 GitHub Pages）

- 修复：**开发者模式拖时间轴时图片加载不出来 / 文字和图对不上**。两个原因叠加：
  ① v1.2.4 的全量预加载（24 张）加上每次切图预热的 3 张邻居图，把连接队列占满，
  当前要看的那张被排在几十个请求后面；② 文字是"一拖就改"、图片是"加载完才改"，中间必然对不上。
  现在：进入开发者模式会**中止预加载**、开发者模式**不再预热邻居图**、拖动改为 120ms 防抖
  （已缓存的那张仍然立刻切换），并且**时期名和背景图在同一时刻一起切**
  ——图没到位就保持旧时期，不会再出现"文字跳到太古代、图还是大爆炸"。
- 修复：结束一段后重新开始，第一张背景沿用上一段（现在会立刻回到第 1 张，已加回归测试）。
- 修复：预加载被中止后重启时，上一轮还在途中的回调会计入新一轮的计数（改为每轮独立计数）。
- 内部：预加载改用 `fetch(url, { priority: 'low' })`，让"正在切的那张图"优先拿到连接。

### v1.2.4（已发布到 GitHub Pages）

- 改进：**背景图一次性全部预加载**——打开页面后把 24 张图（合计约 7.4MB）以 4 并发拉进浏览器缓存，
  切图不再出现"图还没下完、背景不换"的空档；同时只长期持有邻近几张 Image 对象，
  避免 24 张 2560px 解码位图占掉几百 MB 内存。
- 加固：某张背景图加载失败时会自动重试一次，避免偶发网络抖动让该背景一直出不来。

### v1.2.3（已发布到 GitHub Pages）

- 改进：**开发者模式下，计时页那一行显示当前时期名**（大爆炸 / 太阳系 / 侏罗纪…）并跟着时间轴切换，
  不再显示 "Developer Testing"；任务输入框也不再被写入占位文字。

### v1.2.2（已发布到 GitHub Pages）

- 新增：**专注历史可以改任务名（标题）**——在历史记录里点一下任务名就能改，
  `Enter` 保存、`Esc` 取消、点到别处也保存；空名字不会覆盖原值，其余字段（时长、时间、备注）原样保留。
- 修复：**快速连续切图时图片不切换 / 乱切换**——前一张图的加载回调会迟到，
  把背景又改回旧图（拖开发者模式时间轴、连按 `右 Ctrl + →` 时最明显）。
  现在每次切图带一个令牌，过期回调直接丢弃。

### v1.2.1（已发布到 GitHub Pages）

- 修复：开发者模式点"结束/保存"后计时器会在后台"复活"（`exitDevMode()` 会把已停止的会话重新启动）。
- 修复：开发者模式会把 480 分钟和 "Developer Testing" 写进"记住上次设置"与任务建议，
  导致下次正常打开变成 8 小时（调试状态不再参与记忆）。
- 修复：长时间会话会把全部 24 张解码位图留在内存（图片缓存改为上限 6 张、近似 LRU）。
- 修复：换图时刻恰好处于"隐藏相位"时，文字会被永久卡在可见状态。
- 修复：在输入框/备注里按 `Shift + Enter` 会误触发全屏（`空格` 早就有输入保护）。
- 修复：连点删除按钮 / 热力图圆点 / 历史建议三次会误进全屏。
- 加固：本地存储或云端 `data/records.json` 被写坏、被写成非数组时，
  不再让"查看历史记录"卡在"正在加载…"（脏数据按空数据处理）。
- 修复：切图原本只发生在 rAF 循环里，窗口被遮挡/切到后台时 rAF 被节流，20 分钟节点到了却不换图；
  现在切图节点另有 `setTimeout` 链式兜底，并新增 `右 Ctrl + →` 手动强制切下一张
  （手动切图只记一个偏移量，不影响 20 分钟基准，到下一个节点照常再切一张）。
- 新增：**暂存 / 续上一次**——结束时可选"暂存"保留进度（不写记录），主页出现提示条，点"继续"接着跑。
- 新增：**自动恢复**——计时中每 5 秒快照一次（切后台/关闭页面前再补一次），刷新、误关标签页、
  浏览器崩溃后都能在主页点"继续"接上（最多丢几秒）。
- 内部：「每 20 分钟切一张」的基准统一成一个常量 `CYCLE_MS`，只有一个来源。

### v1.2.0（已发布到 GitHub Pages）

- 背景图全部转 WebP：24 张从约 141MB 压到 7.44MB（最长边 2560px，实测 PSNR 40dB+）；
  取消启动时把全部图片读进内存的做法，改为只预热邻近图片。
- 修复：`images/14.png` 缺失导致"部落"这一段背景 404。
- 修复：历史记录里的开始时间记的是打开页面的时间（`sessionStartTimeObj` 写在了函数外）。
- 修复：界面显示"未设置"却能开始，并默认静默计时 60 分钟。
- 修复：删除记录后被云端数据"复活"（删除后又立刻重新拉取云端）。
- 修复：切到后台/休眠后回来，背景图只前进 1 张并永久落后（改为按圈数推导索引，可自愈）。
- 修复：到点提醒依赖 `requestAnimationFrame`，切到后台标签页就不会触发（增加 `setTimeout` 兜底）。
- 修复：下拉建议用字符串拼内联 `onmousedown`，任务名带单引号会弄坏 JS。
- 修复：CSV 导出未按标准转义；导入备份未校验类型；`finishTimer` 重复定义。
- 修复：暂停后背景平移/波纹动画仍在运行、文字呼吸的独立定时器仍在计时，恢复时还会重播开场动画
  （改为 `#timer-screen.paused` 冻结 CSS 动画 + 呼吸相位按已用时长推导）。
- 新增：标签页标题显示剩余时间、记住上次任务与时长、历史记录今日/本周合计、任务建议模糊匹配、版本号。
- 新增：专注热力图（灰/蓝/紫/红四档，可点圆点看当天明细）与连续使用天数 `×N` 徽章。
- 桌面端：`空格` / `Esc` 快捷键、计时中离开页面确认、弹窗内可选中文字。
- 移除：所有音效相关代码（本应用不发声）。
- 清理：删掉 `timing/` 陈旧副本（旧版 `timing.html` + 1 字节空白 `index.html`）。
